apiVersion: v1
kind: Service
metadata:
  name: {{ include "isambard.fullname" . }}-app
  labels:
    {{- include "isambard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.app.type }}
  selector:
    {{- include "isambard.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.app.port }}
      targetPort: http-app
